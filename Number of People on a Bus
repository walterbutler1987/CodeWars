public static int countPassengers(ArrayList<int[]> stops) {
        int[] a;
        int b, c = 0;

        for(int i = 0; i < stops.size(); i++){//access stops
            a = stops.get(i);
            b = a[0] - a[1];
            c += b;
        }
        return c;
    }
